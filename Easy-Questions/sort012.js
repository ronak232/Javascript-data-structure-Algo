var arr = [0, 1, 1, 0, 1, 2, 1, 2, 0, 0, 0, 1];

function sort(arr) {
  let len = arr.length - 1;

  for (let i = 0; i < len; i++) {
    
    
  }
  return arr;
}

let res = sort(arr);
console.log(res);
